/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
	domains: [
      'assets.aceternity.com',
      'images.unsplash.com',
      'localhost',
      'localhost:8000',
    ],
  },
};

module.exports = nextConfig;
